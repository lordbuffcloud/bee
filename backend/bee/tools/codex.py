class CodexHelper:
    def tips(self) -> list[str]:
        return [
            "Use clear, bounded tasks.",
            "Provide expected inputs/outputs.",
            "Keep secrets in env vars, not code.",
        ]
